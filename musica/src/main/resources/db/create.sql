   CREATE TABLE musicShop.musica(
    id text unique PRIMARY KEY NULL,
    title text NOT NULL,
    artists text NOT NULL,
    GeneroMusical text NOT NULL,
    valor Real NOT NULL,
    datalacamento DATE NOT NULL
   )  
